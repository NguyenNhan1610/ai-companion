# todo-list schema — DAG-first, authored by humans/agents, validated by planning-docs.mjs
# Location: .project/todo-lists/TODO-{NN}-{slug}.yaml

# ── Header (strict frontmatter) ─────────────────────────────────────────

id: TODO-03                                   # short-form, matches filename stem
type: todo-list                               # full form, must equal this constant
slug: chat-ui-copilot-experience              # kebab-case
title: Chat UI Copilot Experience
status: active                                # draft | active | superseded | deprecated

# Relationships: full relative paths from repo root, not bare IDs.
upstream:
  - .project/implementation-plans/IMPL-03-chat-ui-copilot-experience.md
downstream: []                                # todo-list is a leaf

supersedes: []                                # not applicable for todo-list
superseded_by: null
created: 2026-04-22
updated: 2026-04-22

# ── DAG block (at top; agents read this first) ──────────────────────────

dependencies:
  - from: task-01
    to: task-03
    kind: hard                                # hard | soft | data
    reason: task-03 consumes the page component created by task-01
  - from: task-02
    to: task-03
    kind: soft
    reason: task-03 benefits from the shared hook extracted in task-02

# computed: regenerated by `/ai:task-graph` — do NOT hand-edit
computed:
  roots: [task-01, task-02]
  ready: [task-01, task-02]
  critical_path: [task-01, task-03]
  critical_path_points: 8
  parallel_tracks:
    - [task-01, task-03]
    - [task-02]

# ── Task bodies ─────────────────────────────────────────────────────────

tasks:
  - id: task-01                               # kebab-case, unique within file
    title: Scaffold chat page route
    status: ready                             # pending | ready | in-progress | blocked | in-review | complete | cancelled
    priority: P1                              # P0 | P1 | P2 | P3
    confidence: 0.9                           # 0..1 float
    effort: M                                 # XS | S | M | L | XL  (points: 1/2/3/5/8)
    acceptance_trace: [EAC-01, FAC-02]        # acceptance criteria IDs from upstream
    evidence: []                              # { file, line?, kind } — kind ∈ implementation|test|doc
    ticket: null
    assigned_to: null
    notes: ""

  - id: task-03
    title: Wire SSE stream into chat UI
    status: pending
    priority: P0
    confidence: 0.7
    effort: L
    acceptance_trace: [EAC-04]
    evidence: []
    ticket: LIN-234
    assigned_to: null
    notes: ""

# ── Validation rules (enforced by planning-docs.mjs) ────────────────────
# - id must match filename stem
# - type must equal "todo-list"
# - upstream entries must be relative paths that exist
# - every dependency.from / dependency.to must be a declared task id
# - no self-loops, no cycles across any edge kind
# - priority ∈ {P0,P1,P2,P3}; effort ∈ {XS,S,M,L,XL}; confidence ∈ [0,1]
# - evidence entries require `file`; optional `line`, `kind`
# - acceptance_trace entries are free-form ID tokens (AAC/FAC/TC/EAC short form)
